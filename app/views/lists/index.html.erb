<nav class="navbar bg-body-tertiary" style="height: 8vh">
  <div class="container-fluid d-flex justify-content-between">
    <div>
      <%= link_to root_path, class: "navbar-brand mb-0 h1" do %>
      <i class="fa-solid fa-film"></i>
      <% end %>
    </div>
    <div>
      <%= link_to "My Lists", lists_path, style: "text-decoration: none; color: black;" %>
      <%= link_to "New List", new_list_path, style: "text-decoration: none; color: black;" %>
    </div>
  </div>
</nav>
<div class="m-3">
<h1 style="text-align: center;">Lists</h1>
<ul class="p-2">
  <% @lists.each do |list| %>
    <div class="card-category m-2" style="background-image: url('<%= url_for(list.photo) %>')">
      <li><%= link_to list.name, list_path(list)%></li>
    </div>
  <% end %>
</ul>
</div>
</div>
